# Runtime-only overrides mounted by docker-compose in production.
# Keep this file secret-free; copy to .deploy/runtime.env when needed.

NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://example.com
AUTH_URL=https://example.com
SENTRY_ENVIRONMENT=production
MAINTENANCE_MODE=false

